#!/usr/bin/env bash
echo "Downloading..."
npm install
echo "Installing..."
cpath="$(dirname $(readlink -f $0))"
$cpath/../node_modules/.bin/grunt install \
    --method=path --rpath=/usr/local \
    --rc=`bash < "$cpath/locrc"` \
    --minify