#!/usr/bin/env bash
echo "Downloading..."
npm install
echo "Installing..."
cpath="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
$cpath/../node_modules/.bin/grunt install \
    --method=path --rpath=/usr/local \
    --rc=`bash < "$cpath/locrc"` \
    --minify