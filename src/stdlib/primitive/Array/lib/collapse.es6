export default (api) => ["collapse", api.var(new api.func(
    [
        ["dimensions", {
            Type: api.string,
            Splat: true
        }]
    ],
    function(scope, input) {
        // TODO:
    }
))];
