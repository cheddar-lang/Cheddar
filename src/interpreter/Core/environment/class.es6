import CheddarScope from './scope';

export default class CheddarClass extends CheddarScope {
    static Operators = new Map()
    constructor( /* TODO: figure args for classes out */ ) {
        super();
    }
}