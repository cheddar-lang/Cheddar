#!/usr/bin/env sh

base=`dirname "$0"`
rel="$base/cheddar.js"
CHEDDAR_EXE=$([[ -f "$rel" ]] && echo "$rel" || echo /usr/bin/share/cheddar/cli/cheddar.js)
NODE_EXE=`which node`
"$NODE_EXE" "$CHEDDAR_EXE" "$@"