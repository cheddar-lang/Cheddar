for (let i := 2; i <= 1000; i += 1) {
  let found := false;
  for (var d := 2; d <= sqrt i; d += 1) {
    if (i % d == 0) {
      found = true;
      break;
    }
  }
  if (!found) {
    print i;
  }
}
