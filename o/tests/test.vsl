# Test Class

object Age(Int: age) {
  days {
    GET floor(age * 365.25)
    SET age self / 365.25
  }
}

class Person(String: name, Int: age) {
  main {
    self name name
    self age  Age:(age)
  }
}

def PersonA Person:("Frank", 23)
print PersonA.age.days # Outputs 8,400
personA.age.days := 8766
print PersonA.age # Outputs 24